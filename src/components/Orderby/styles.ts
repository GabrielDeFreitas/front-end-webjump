import styled from 'styled-components'

export const OrderBy = styled.main`
  display: flex;
  justify-content: end;
  align-items: center;
  gap: 0.75rem;

  div {
    color: #808185;
    font-size: 14px;
    font-weight: 400;
  }

  select {
    width: auto;
  }
`
